import Nav from "./Nav";
import Header from "./Header";

const Homepage = () => {
    return (
        <div>
            <Header pageName={'Home'}/>
            <Nav />
        </div>
    );
};

export default Homepage;