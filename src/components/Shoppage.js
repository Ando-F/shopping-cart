import Nav from "./Nav";
import Header from "./Header";

const Shoppage = () => {
    return (
        <div>
            <Header pageName={'Shop'}/>
            <Nav />
        </div>
    );
};

export default Shoppage;